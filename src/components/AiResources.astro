<div class="ai-resources-wrap">
  <section class="ai-resources" aria-label="AI resources">
    <span class="ai-resources-watermark" aria-hidden="true">AI<canvas class="ai-network-canvas" aria-hidden="true"></canvas></span>
    <a class="ai-resources-primary" href="https://github.com/pubky/agent-skills" target="_blank" rel="noopener noreferrer" aria-label="Agent skills (opens in new tab)">
      <strong>Agent skills</strong>
      <span>Pubky workflows for your LLM coding agents</span>
    </a>
    <div class="ai-resources-group">
      <a class="ai-resources-title" href="https://context7.com/?q=pubky" target="_blank" rel="noopener noreferrer" aria-label="Search Pubky on Context7 (opens in new tab)">
        <strong>Context7</strong>
        <span>API documentation and examples.</span>
      </a>
      <div class="ai-resources-repositories">
        <a href="https://context7.com/pubky/pubky-homeserver" target="_blank" rel="noopener noreferrer" aria-label="Pubky Homeserver on Context7 (opens in new tab)">Homeserver</a>
        <a href="https://context7.com/pubky/pkarr" target="_blank" rel="noopener noreferrer" aria-label="PKARR on Context7 (opens in new tab)">PKARR</a>
      </div>
    </div>
    <a href="https://deepwiki.com/pubky/pubky-homeserver" target="_blank" rel="noopener noreferrer" aria-label="Pubky Homeserver on DeepWiki (opens in new tab)">
      <strong>DeepWiki</strong>
      <span>AI explanations of the Pubky codebase.</span>
    </a>
  </section>
</div>

<script>
  import '../scripts/ai-network-animation';
</script>

<style>
  /* Match the resource card to the original SDK terminal width. */
  .ai-resources-wrap {
    position: relative;
    width: 500px;
    max-width: 100%;
  }
  /* A visible resource box uses the existing terminal palette and geometry. */
  .ai-resources {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: var(--spacing-3);
    width: 500px;
    max-width: 100%;
    min-height: calc(var(--text-6xl-lh) * 4.5 + var(--spacing-6));
    box-sizing: border-box;
    padding: var(--spacing-4);
    border: var(--landing-border-width) solid var(--base-border);
    border-radius: var(--radius-xl);
    background: var(--base-background);
  }
  /* Oversized type carries the decorative mesh behind the resource links. */
  .ai-resources-watermark {
    position: absolute;
    z-index: 0;
    top: 50%;
    right: var(--spacing-4);
    transform: translateY(-50%) rotate(-12deg);
    font-family: "Inter Tight", "Inter", system-ui, sans-serif;
    font-size: calc(var(--text-6xl-size) * 4.05);
    font-weight: 700;
    line-height: calc(var(--text-6xl-lh) * 4.05);
    letter-spacing: calc(var(--spacing-2) * -1);
    color: transparent;
    pointer-events: none;
    user-select: none;
  }
  .ai-network-canvas {
    position: absolute;
    inset: 0;
    z-index: 1;
    display: none;
    width: 100%;
    height: 100%;
    opacity: 0.34;
    pointer-events: none;
  }
  .ai-resources[data-ai-network-ready] .ai-network-canvas { display: block; }
  .ai-resources[data-ai-network-ready] .ai-resources-watermark::before { opacity: 0; }
  /* Without canvas, only the clipped net defines the letters; there is no fill. */
  .ai-resources-watermark::before {
    z-index: 2;
    content: 'AI';
    position: absolute;
    inset: 0;
    font: inherit;
    letter-spacing: inherit;
    color: transparent;
    background-clip: text;
    -webkit-background-clip: text;
    pointer-events: none;
  }
  .ai-resources-watermark::before {
    opacity: 0.8;
    background-image:
      repeating-linear-gradient(
        60deg,
        var(--alpha-white-16) 0 var(--landing-border-width),
        transparent var(--landing-border-width) var(--spacing-2)
      ),
      repeating-linear-gradient(
        -60deg,
        var(--alpha-white-16) 0 var(--landing-border-width),
        transparent var(--landing-border-width) var(--spacing-2)
      );
  }
  .ai-resources > a,
  .ai-resources-group {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    gap: var(--spacing-1);
    padding: var(--spacing-2);
  }
  .ai-resources a {
    min-width: var(--landing-touch-target);
    min-height: var(--landing-touch-target);
    box-sizing: border-box;
    border-radius: var(--radius-sm);
    color: var(--base-foreground);
    text-decoration: none;
    transition: background var(--landing-motion) ease;
  }
  .ai-resources a:hover { background: var(--alpha-white-5); }
  .ai-resources strong {
    font-size: var(--text-sm-size);
    line-height: var(--text-sm-lh);
    font-weight: 600;
  }
  .ai-resources > a > span,
  .ai-resources-title > span {
    font-size: var(--text-xs-size);
    line-height: var(--text-xs-lh);
    color: var(--base-secondary-foreground);
  }
  /* A single frame and aligned copy keep Skills prominent without nested panels. */
  .ai-resources .ai-resources-primary {
    gap: var(--spacing-2);
  }
  .ai-resources .ai-resources-primary strong {
    font-family: "Inter Tight", "Inter", system-ui, sans-serif;
    font-size: var(--text-2xl-size);
    line-height: var(--text-2xl-lh);
    font-weight: 700;
  }
  .ai-resources .ai-resources-primary > span {
    font-size: var(--text-sm-size);
    line-height: var(--text-sm-lh);
  }
  .ai-resources-title {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: var(--spacing-1);
    width: fit-content;
  }
  .ai-resources-repositories { display: flex; flex-wrap: wrap; gap: var(--spacing-4); }
  .ai-resources-repositories a {
    display: inline-flex;
    align-items: flex-start;
    padding-top: var(--spacing-1);
    font-size: var(--text-xs-size);
    line-height: var(--text-xs-lh);
    text-decoration: underline;
    text-underline-offset: var(--spacing-1);
  }

  @media (max-width: 1049px) {
    .ai-resources-watermark {
      font-size: calc(var(--text-6xl-size) * 3.0375);
      line-height: calc(var(--text-6xl-lh) * 3.0375);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ai-network-canvas { display: none; }
    .ai-resources a { transition: none; }
  }
</style>
