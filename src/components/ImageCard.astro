---
interface Props {
  title: string;
  titleSuffix?: string;
  titleIcon?: string;
  description?: string;
  image: string;
  tags?: { label: string; href: string }[];
  links?: { label: string; href: string; external?: boolean }[];
}

import { withBase } from '../utils/base-path';

const { title, titleSuffix, titleIcon, description, image, tags = [], links = [] } = Astro.props;
const imageSrc = withBase(image);
const hrefFor = (href: string, external = href.startsWith('http')) => external ? href : withBase(href);
---

<div class="image-card">
  <div class="image-card-thumbnail">
    <img src={imageSrc} alt={title} loading="lazy" />
  </div>
  <div class="image-card-content">
    <h3 class="image-card-title">
      {titleIcon && <span class="image-card-title-icon" set:html={titleIcon} />}
      <span class="image-card-title-text">
        {title}{titleSuffix && <> <span class="image-card-title-suffix">{titleSuffix}</span></>}
      </span>
    </h3>
    {description && <p class="image-card-description">{description}</p>}
    {tags.length > 0 && (
      <div class="image-card-tags">
        {tags.map((tag) => (
          <a href={hrefFor(tag.href)} class="image-card-tag">{tag.label}</a>
        ))}
      </div>
    )}
    {links.length > 0 && (
      <div class="image-card-links">
        {links.map((link) => (
          <a
            href={hrefFor(link.href, link.external)}
            class="image-card-link"
            target={link.external ? "_blank" : undefined}
            rel={link.external ? "noopener noreferrer" : undefined}
            aria-label={link.external ? `${link.label} (opens in new tab)` : undefined}
          >
            {link.label}
          </a>
        ))}
      </div>
    )}
  </div>
</div>

<style is:global>
  /* Figma: Pubky / Cards / Image (node 32394:120859) */
  .image-card {
    display: flex;
    flex-direction: column;
    /* Figma: gap = spacing/4 (16px) */
    gap: var(--spacing-4);
    /* Figma: padding = spacing/5 (20px) */
    padding: var(--spacing-5);
    background: var(--base-background);
    /* Figma: border = 1px base/border */
    border: 1px solid var(--base-border);
    /* Figma: border-radius = rounded-lg (12px) */
    border-radius: var(--radius-lg);
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
    overflow: hidden;
  }

  .image-card:hover {
    border-color: var(--sl-color-gray-3);
    box-shadow: var(--shadow-card-hover);
  }

  /* Figma: Thumbnail = black bg, rounded-lg, aspect 352/187 */
  .image-card-thumbnail {
    border-radius: var(--radius-lg);
    overflow: hidden;
    aspect-ratio: 352 / 187;
    background: var(--base-background);
    flex-shrink: 0;
  }

  .image-card-thumbnail img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  /* Figma: flex-col, gap spacing/4 (16px), items-start — items stack from the top with
     a fixed gap; any extra card height stays empty at the bottom (no space-between). */
  .image-card-content {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-4);
  }

  /* Figma: Typography / H4 = Inter Tight Bold, text-xl(20/28), foreground */
  .image-card-title {
    margin: 0;
    font-size: var(--text-xl-size);
    line-height: var(--text-xl-lh);
    font-weight: 700;
    color: var(--base-foreground);
    display: flex;
    align-items: center;
    gap: var(--spacing-2);
  }

  .image-card-title-icon {
    display: inline-flex;
    color: var(--base-foreground);
    flex-shrink: 0;
  }

  /* Figma: "Ring" word mark in Pubky Ring logo renders thinner than the bold "Pubky" */
  .image-card-title-suffix {
    font-weight: 400;
  }

  /* Figma: Typography / P = text-base (16px), line-height 24px, muted-foreground, medium */
  .image-card-description {
    margin: 0;
    font-size: var(--text-base-size);
    line-height: var(--text-base-lh);
    color: var(--base-muted-foreground);
    font-weight: 500;
  }

  .image-card-tags {
    display: flex;
    flex-wrap: wrap;
    /* Figma: gap = 8px */
    gap: var(--spacing-2);
  }

  /* Figma: Shared pill button = h-40, rounded-full, text-sm bold, shadow-xs */
  .image-card-tag,
  .image-card-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    /* Figma: h = 40px, px = spacing/4 (16px) */
    height: 40px;
    padding: 0 var(--spacing-4);
    background: var(--alpha-white-5);
    border: 1px solid var(--base-border);
    border-radius: var(--radius-full);
    color: var(--base-foreground);
    font-size: var(--text-sm-size);
    line-height: var(--text-sm-lh);
    font-weight: 700;
    text-decoration: none;
    box-shadow: var(--shadow-xs);
    transition: background 0.15s ease, border-color 0.15s ease;
  }

  .image-card-tag:hover,
  .image-card-link:hover {
    background: var(--alpha-white-10);
    border-color: var(--sl-color-gray-4);
  }

  /* Figma: Flex row, flex-wrap, gap 8px */
  .image-card-links {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-2);
  }

  /* Figma: Button secondary variant = bg base/secondary, color secondary-foreground, no border */
  .image-card-link:not(:first-child) {
    background: var(--base-secondary);
    border-color: transparent;
    color: var(--base-secondary-foreground);
  }

  .image-card-link:not(:first-child):hover {
    background: var(--base-secondary-hover);
    border-color: transparent;
  }
</style>
